<?xml version="1.0" encoding="UTF-8"?>
<!--
  项目内 Maven 配置，不修改用户全局 ~/.m2/settings.xml。
  由 scripts/env.sh 通过 MAVEN_ARGS 生效（Maven 3.9+ 支持该环境变量）。

  两点说明：

  1. 用镜像不是为了图快。直连 repo.maven.apache.org 在当前网络下会随机出现
     "Remote host terminated the handshake"，构建结果因此不可复现。
     行内环境应替换为行内 Nexus/Artifactory 地址。

  2. mirror 的 id 刻意沿用 "central"。若换成别的 id，本地仓里已下载工件的
     _remote.repositories 记录会与新仓库 id 对不上，触发全量重新解析。
-->
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 https://maven.apache.org/xsd/settings-1.0.0.xsd">

  <mirrors>
    <mirror>
      <id>central</id>
      <name>Aliyun Public Mirror</name>
      <url>https://maven.aliyun.com/repository/public</url>
      <mirrorOf>central</mirrorOf>
    </mirror>
  </mirrors>

</settings>
