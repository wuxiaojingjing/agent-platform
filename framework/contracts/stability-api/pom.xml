<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.huawei.finance</groupId>
    <artifactId>agent-platform</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <!-- 移进 framework/ 一层后父 pom 不再是默认的 ../pom.xml -->
    <relativePath>../../../pom.xml</relativePath>
  </parent>

  <artifactId>stability-api</artifactId>
  <name>stability-api</name>
  <description>稳定性契约标注：@Api / @Spi。零依赖，供所有平台模块使用</description>

  <!--
    刻意没有任何依赖。这个模块要被全部其它模块依赖，一旦它自己引入了什么，
    那个东西就成了全工程的强制传递依赖。三个注解不需要任何库。
  -->
</project>
