agent:
  id: agent.enterprise_service
  displayName: 企业助手
  roles: [domain]
  domains: [enterprise_service]
implementation: { mode: scaffold }
runtime: { intentEngine: default, context: enabled, taskOrchestrator: enabled }
a2a: { inbound: true, delegation: { enabled: true } }
assets: { path: ./assets }
storage: { namespace: enterprise-service }
