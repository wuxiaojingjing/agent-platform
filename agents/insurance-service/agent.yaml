agent:
  id: agent.insurance_service
  displayName: 保险助手
  roles: [domain]
  domains: [insurance_service]
implementation: { mode: extension, artifact: "com.huawei.finance:insurance-service" }
runtime: { intentEngine: default, context: enabled, taskOrchestrator: enabled }
a2a: { inbound: true, delegation: { enabled: true } }
assets: { path: ./assets }
storage: { namespace: insurance-service }
